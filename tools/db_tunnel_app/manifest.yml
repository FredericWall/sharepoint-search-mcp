applications:
  - name: ((tunnel-app-name))
    path: .
    memory: 128M
    disk_quota: 512M
    instances: 1
    no-route: true
    health-check-type: process
    buildpacks:
      - python_buildpack
    command: python -m tunnel_host
    services:
      - ((postgres-service))
